/* exported getValue */
// Define function
// ~ Defined a new function named getValue with two parameters
// one being an object, and the second being a key
// Return result
// ~ Returned object at key

function getValue(object, key) {
  return object[key];
}
