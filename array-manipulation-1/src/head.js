/* exported head */
// Define the function
//  ~ Defined the function head with 1 parameter array - Completed
// Create a empty array
//  ~ Created a variable named emptyArray with a value of an empty Array - Completed
// Use the push method
//  ~ Used the push method to push the array into the variable EmptyArray
// With the index of array being [0] - Completed
// Return result
// ~ Returned the variable emptyArray with an index of 0 - Completed

function head(array) {
  var emptyArray = [];
  emptyArray.push(array[0]);
  return emptyArray[0];
}
