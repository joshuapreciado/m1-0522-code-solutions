/* exported isVowel */
function isVowel(character) {
  return character === 'e' || character === 'E' || character === 'i' || character === 'I' ||
    character === 'a' || character === 'o' || character === 'u' || false;
}
