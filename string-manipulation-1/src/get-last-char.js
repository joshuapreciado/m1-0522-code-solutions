/* exported getLastChar */
// Define function
// ~ Defined a new function named getLastChar
// with a parameter of string - Completed
// Return the result
// ~ Returned string at string.length being subtracted
// by - 1 to target the last index of the string - Completed

function getLastChar(string) {
  return string[string.length - 1];
}
